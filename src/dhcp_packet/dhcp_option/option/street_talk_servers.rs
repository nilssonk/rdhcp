#[derive(Debug, Clone, PartialEq)]
pub struct StreetTalkServers;
        
impl StreetTalkServers {
    pub(crate) fn decode(data: &[u8]) -> Option<crate::DhcpOption> {
        unimplemented!();
    }
}
